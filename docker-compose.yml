volumes:
  pg_data:
  static:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    build: ./backend/
    env_file: .env
    depends_on:
      - db
    volumes:
      - static:/backend_static
  frontend:
    build: ./frontend/
    volumes:
      - static:/frontend_static
  gateway:
    build: ./nginx/
    depends_on:
      - backend
    ports:
      - 8001:80
    volumes:
      - static:/static
